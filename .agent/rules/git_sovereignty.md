# Git Sovereignty Rule

## Description
AI models and agents are strictly prohibited from performing automatic version control operations.

## Rules
1. **No Automatic Git**: NEVER execute `git commit` or `git push` automatically.
2. **Explicit Authorization**: All versioning actions must be explicitly authorized by the USER in the current session.
3. **No Assumptions**: Do not assume "save progress" or "record status" includes git operations.

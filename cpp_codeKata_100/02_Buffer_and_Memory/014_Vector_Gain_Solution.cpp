#include <iostream>
#include <vector>

/*
 * ==============================================================================
 * KATA 014 的解答 (Reference Solution)
 * ==============================================================================
 */

void applyGain(std::vector<float>& buffer, float gain) {
    // ------------------------------------------------------------------
    // 解答：
    for (float& sample : buffer) {
        sample *= gain;
    }
    
    /* 
    補充：在 C++ 中使用「範圍 for 迴圈 (Range-based for loop)」配合引用 (&) 
    是修改整個容器內容最簡潔且安全的方式。
    */
    // ------------------------------------------------------------------
}

int main() {
    std::cout << "這是解答檔，請自行對照！\n";
    return 0;
}

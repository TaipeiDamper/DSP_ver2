# 2026-02-24 架構深耕：指標與引用規範化、Block 接口全面落位

## 1. 今日達成里程碑 (Achievements)
- **DSP 架構邏輯定案 (Ref vs Ptr)**：
    - 確立了「管理者 (Supervisor) vs. 執行者 (Worker)」的論點。
    - **引用 (&)**：用於 Level 2/3 管理物件與容器，確保非空安全性與跨部門通訊。
    - **指標 (*)**：用於 Level 0/1 處理原始樣本流水線，追求極致效能與 SIMD 優化友善性。
- **核心接口標準化 (processBlock Implementation)**：
    - **Level 1 (delay_comb_allpass.h)**：為 `Delay` 類別實作了 `processBlock`，並修正了「名稱遮蔽 (Name Shadowing)」潛在風險（將參數改名為 `inputBuffer` 避開成員 `buffer`）。
    - **繼承優勢實踐**：在 `FFCombFilter` 中定義 `processBlock`，使子類別 `FBCombFilter` 與 `APF_delayLine` 自動獲得批次處理能力，體現了優良的物件導向設計。
    - **Level 2 (reverb_fromCombAllpass.h)**：補完 `SchroederReverb` 的 `processBlock`。採用 In-place 原地處理流程，最大化快取局部性 (Cache Locality)。
- **ParamSmoother 2.0 (Target-based) 重構完成**：
    - 將 Smoother 從「濾波器模式」轉向「目標值模式 (Target-based)」。
    - 實作了 `setTarget()` 與 `processSample()` 的解耦，解決了音訊線程頻繁傳遞參數的負擔。
    - **安全性加固**：加入了 `setTimeConst` 的安全下限檢查（防止除以零）以及 `std::abs` 閾值門檻。
    - **功能擴展**：新增 `jumpToTarget()` 以支援參數瞬跳需求。
- **DSP 驗證自動化**：
    - 建立並運行 `test_smoother.cpp`，成功驗證指數平滑的階躍響應 (Step Response) 與區塊處理正確性。
- **內存擁有權邏輯釐清**：
    - 明確了「組合 (Composition)」關係下，內部組件應直接呼叫而非透過指標，維持代碼簡潔與內存連續性。

## 2. 核心架構口訣 (Architecture Mnemonic)
> **「內部擁有直接用，外部手動看工種；數據工頭拿指標，資源分包交引用。」**
> 
> 1. **內部關係 (Ownership)**：A 擁有 B (器官)，直接以 Value 形式組合，正常呼叫。
> 2. **外部關係 (Interface)**：A、B 互不隸屬 (通訊)：
>    - **動手做事 (Worker)**：涉及樣本掃描、效能優先時，傳遞 **`Ptr (*)`**。
>    - **傳遞資源 (Supervisor)**：涉及物件傳包、確保存在時，傳遞 **`Ref (&)`**。

## 3. 技術評價與分析 (Strategist Analysis)
- **架構純粹度**：**極高**。Level 0-2 的模組現在擁有一致且專業的工業級接口，且完全不依賴於特定框架 (Framework-agnostic)。
- **效能潛力**：透過 `float*` 指標與 In-place 處理，為編譯器的自動向量化鋪平了道路。
- **學習曲線評估**：USER 對於 C++ 底層內存模型的理解已從「堪用」進化到「具備架構判斷力」，能主動識別物件擁有權與通訊邊界的差異。

## 3. 遺留問題與下階段目標 (Next Steps)
- **ParamSmoother 效能優化**：已完成目標值架構重構與一次乘法優化。
- **Biquad Filter 驗證**：
    - 雖然基礎類別已存在，但需建立 `test_filter.cpp` 驗證其頻率響應（LPF, HPF 等）。
- **Real-time Safety 靜態檢查**：
    - 確保所有新寫入的 `processBlock` 迴圈中皆無隱藏的分配或阻斷操作。

---
*今日衝刺時數：約 2 小時*
*代碼主權：100% (USER 驅動，AI 軍師輔助診斷)*
